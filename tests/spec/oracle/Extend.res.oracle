[kernel] Parsing Extend.i (no preprocessing)
[kernel:annot-error] Extend.i:31: Warning: unexpected token '\test::baz'
[kernel:annot-error] Extend.i:37: Warning: unexpected token 'baz'
[kernel] Reparsing file
/* Generated by Frama-C */
int f(int x);

/*@ behavior test:
      \test::foo y ≡ 1;
      \test::bar \old(y) + \result ≡ 0;
      \test::bla \trace(3) ∧ \trace(2);
 */
int g(int y);

/*@ \test::foo x ≡ 0;
    \test::bar \result ≡ 0;
    \test::bla \trace(6) ∨ \trace(5);
 */
int f(int x)
{
  int s = 0;
  {
    int i = 0;
    /*@ loop \test::lfoo i ≤ x;
        loop \test::baz \at(i,LoopEntry), 0; */
    while (i < x) {
      int tmp;
      tmp = g(i);
      s += tmp;
      i ++;
    }
  }
  /*@ \test::ca_foo s ≡ 0; */ ;
  return s;
}

int k(int z)
{
  int tmp;
  int x = z;
  int y = 0;
  /*@ \test::ns_foo \at(x,Post) ≡ z + 1; */
  {
    tmp = x;
    x ++;
    y = tmp;
  }
  return y;
}

/*@ \test::global_foo ∀ ℤ x; x < x + 1;
*/
/*@ behavior ca_foo:
      ensures ca_foo: \true; */
void loop(void)
{
  /*@ for ca_foo: \test::ca_foo \true; */ ;
  /*@ \test::ns_foo \true; */
  /*@ \test::baz \true; */
  /*@ \test::empty_extension ; */
  /*@ loop invariant \true; */
  while (1) break;
  return;
}


