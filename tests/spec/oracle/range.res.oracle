[kernel] Parsing range.c (with preprocessing)
[kernel] Parsing range_reparse.c (with preprocessing)
/* Generated by Frama-C */
struct foo {
   char bar[4] ;
};
typedef char baz[4];
struct bli {
   baz bli ;
};
/*@ logic set<ℤ>  range(ℤ x, ℤ y) = (x .. y);
 */
/*@ assigns x->bar[0 .. 3];
    assigns x->bar[0 .. 3] \from x->bar[0 .. 3]; */
int f(struct foo *x);

/*@ assigns *(x + (0 .. 3));
    assigns *(x + (0 .. 3)) \from y->bli[0 .. 3]; */
int g(char x[4], struct bli *y);

/*@ ensures \let range = (0 .. 4 - 1); *(\old(x) + range) ≡ 0;
    assigns *(x + (0 .. (1 << 2) - 2));
    assigns *(x + (0 .. (1 << 2) - 2))
      \from \let range = (0 .. 4 - 1); *(x + range);
 */
int h(char x[4]);


