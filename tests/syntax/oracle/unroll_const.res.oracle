[kernel] Parsing unroll_const.i (no preprocessing)
/* Generated by Frama-C */
struct __anonstruct_u_1 {
   int i1 ;
   int i2 ;
};
struct v {
   int k ;
   int l ;
};
struct s {
   int i ;
   int j ;
   struct v v ;
};
int const t[4] = {4, 2};
struct __anonstruct_u_1 const u[3] =
  {{.i1 = 2, .i2 = 5}, {.i1 = 3, .i2 = 7}};
struct s const s = {.i = 5, .j = 8, .v = {.k = 3, .l = 0}};
int volatile c;
void main(void)
{
  unsigned int i = (unsigned int)0;
  if (! c) goto unfolding_2_loop;
  i ++;
  unfolding_6_loop: ;
  if (! c) goto unfolding_2_loop;
  i ++;
  unfolding_5_loop: ;
  if (! c) goto unfolding_2_loop;
  i ++;
  unfolding_4_loop: ;
  if (! c) goto unfolding_2_loop;
  i ++;
  unfolding_3_loop: ;
  /*@ loop unfold sizeof(t) / sizeof(t[0]);
      loop unfold "done", 4; */
  while (c) i ++;
  unfolding_2_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_20_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_19_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_18_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_17_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_16_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_15_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_14_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_13_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_12_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_11_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_10_loop: ;
  if (! c) goto unfolding_8_loop;
  i ++;
  unfolding_9_loop: ;
  /*@ loop unfold \offset(&s.v.l);
      loop unfold "done", 12; */
  while (c) i ++;
  unfolding_8_loop: ;
  if (! c) goto unfolding_22_loop;
  i ++;
  unfolding_27_loop: ;
  if (! c) goto unfolding_22_loop;
  i ++;
  unfolding_26_loop: ;
  if (! c) goto unfolding_22_loop;
  i ++;
  unfolding_25_loop: ;
  if (! c) goto unfolding_22_loop;
  i ++;
  unfolding_24_loop: ;
  if (! c) goto unfolding_22_loop;
  i ++;
  unfolding_23_loop: ;
  /*@ loop unfold s.i + s.v.l;
      loop unfold "done", 5; */
  while (c) i ++;
  unfolding_22_loop: ;
  if (! c) goto unfolding_29_loop;
  i ++;
  unfolding_33_loop: ;
  if (! c) goto unfolding_29_loop;
  i ++;
  unfolding_32_loop: ;
  if (! c) goto unfolding_29_loop;
  i ++;
  unfolding_31_loop: ;
  if (! c) goto unfolding_29_loop;
  i ++;
  unfolding_30_loop: ;
  /*@ loop unfold \max(t[..]);
      loop unfold "done", 4; */
  while (c) i ++;
  unfolding_29_loop: ;
  /*@ loop unfold \min(t[..]); */
  while (c) i ++;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_41_loop: ;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_40_loop: ;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_39_loop: ;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_38_loop: ;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_37_loop: ;
  if (! c) goto unfolding_35_loop;
  i ++;
  unfolding_36_loop: ;
  /*@ loop unfold \max({1, 1 + s.i});
      loop unfold "done", 6; */
  while (c) i ++;
  unfolding_35_loop: ;
  if (! c) goto unfolding_43_loop;
  i ++;
  unfolding_45_loop: ;
  if (! c) goto unfolding_43_loop;
  i ++;
  unfolding_44_loop: ;
  /*@ loop unfold \min(t[{1, 3}]) + \max(t[{1, 3}]);
      loop unfold "done", 2; */
  while (c) i ++;
  unfolding_43_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_53_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_52_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_51_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_50_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_49_loop: ;
  if (! c) goto unfolding_47_loop;
  i ++;
  unfolding_48_loop: ;
  /*@ loop unfold \min(u[.. 1].i1) * \max(u[.. 1].i1);
      loop unfold "done", 6;
  */
  while (c) i ++;
  unfolding_47_loop: ;
  return;
}


