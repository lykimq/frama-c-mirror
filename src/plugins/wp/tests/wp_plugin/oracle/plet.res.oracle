# frama-c -wp [...]
[kernel] Parsing plet.i (no preprocessing)
[wp] Running WP plugin...
------------------------------------------------------------
  Axiomatic 'Test'
------------------------------------------------------------

Goal Lemma 'GOAL':
Prove: (P_A \/ (exists i : Z. (0 <= i) /\ (i < L_N) /\ (!P_H(i)))) <->
    (((forall i : Z. ((0 <= i) -> ((i < L_N) -> P_H(i)))) -> P_A)).

------------------------------------------------------------
